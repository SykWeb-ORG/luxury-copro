(function(){
'use strict';
var toggle = document.getElementById('themeToggle');
if (toggle) {
toggle.addEventListener('click', function(){
document.body.classList.toggle('light');
localStorage.setItem('ez-theme', document.body.classList.contains('light') ? 'light' : 'dark');
});
}
var hamburger = document.getElementById('hamburger');
var mobileMenu = document.getElementById('mobileMenu');
if (hamburger && mobileMenu) {
hamburger.addEventListener('click', function(){
hamburger.classList.toggle('open');
mobileMenu.classList.toggle('open');
document.body.style.overflow = mobileMenu.classList.contains('open') ? 'hidden' : '';
});
var mmClose = document.getElementById('mmClose');
if (mmClose) {
mmClose.addEventListener('click', function(){
hamburger.classList.remove('open');
mobileMenu.classList.remove('open');
document.body.style.overflow = '';
});
}
document.querySelectorAll('.mm-link').forEach(function(link){
link.addEventListener('click', function(){
hamburger.classList.remove('open');
mobileMenu.classList.remove('open');
document.body.style.overflow = '';
});
});
}
var loaderLogo = document.getElementById('loaderLogo');
if (loaderLogo) {
var brand = 'LUXURY COPRO';
brand.split('').forEach(function(c, i){
var s = document.createElement('span');
s.textContent = c === ' ' ? ' ' : c;
s.style.animationDelay = i * 0.04 + 's';
loaderLogo.appendChild(s);
});
window.addEventListener('load', function(){
setTimeout(function(){
var loader = document.getElementById('loader');
if (loader) loader.classList.add('done');
}, 1200);
});
}
var dot = document.getElementById('cDot');
var ring = document.getElementById('cRing');
if (dot && ring) {
var mx = 0, my = 0, rx = 0, ry = 0;
document.addEventListener('mousemove', function(e){ mx = e.clientX; my = e.clientY; });
(function loop(){
rx += (mx - rx) * 0.12;
ry += (my - ry) * 0.12;
dot.style.transform = 'translate(' + (mx - 3) + 'px,' + (my - 3) + 'px)';
ring.style.transform = 'translate(' + (rx - 22) + 'px,' + (ry - 22) + 'px)';
requestAnimationFrame(loop);
})();
}
var nav = document.getElementById('nav');
var scrollProgress = document.getElementById('scrollProgress');
function updateNavHeight() {
if (nav) document.documentElement.style.setProperty('--nav-h', nav.offsetHeight + 'px');
}
if (nav) {
updateNavHeight();
window.addEventListener('resize', updateNavHeight);
window.addEventListener('scroll', function(){
nav.classList.toggle('compact', window.scrollY > 60);
updateNavHeight();
if (scrollProgress) {
var scrollTop = window.scrollY;
var docHeight = document.documentElement.scrollHeight - window.innerHeight;
scrollProgress.style.width = (scrollTop / docHeight * 100) + '%';
}
});
}
var prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
if (typeof gsap !== 'undefined' && typeof ScrollTrigger !== 'undefined' && !prefersReducedMotion) {
gsap.registerPlugin(ScrollTrigger);
var isMobile = window.innerWidth <= 768;
function apply3dChars(titleEl, triggerEl) {
if (!titleEl || isMobile) {
if (titleEl) gsap.fromTo(titleEl, { opacity: 0, y: 30 }, { scrollTrigger: { trigger: triggerEl || titleEl, start: 'top 85%', once: true }, opacity: 1, y: 0, duration: 0.9, ease: 'power3.out' });
return;
}
var html = titleEl.innerHTML;
var w = '', inTag = false;
for (var i = 0; i < html.length; i++) {
var c = html[i];
if (c === '<') { inTag = true; w += c; continue; }
if (c === '>') { inTag = false; w += c; continue; }
if (inTag) { w += c; continue; }
w += c === ' ' ? '<span class="char-3d space"> </span>' : '<span class="char-3d">' + c + '</span>';
}
titleEl.innerHTML = w;
titleEl.style.transformStyle = 'preserve-3d';
gsap.fromTo(titleEl.querySelectorAll('.char-3d'), { opacity: 0, rotateY: 90, rotateX: -20, z: -80, scale: 0.5 }, {
scrollTrigger: { trigger: triggerEl || titleEl, start: 'top 85%', once: true },
opacity: 1, rotateY: 0, rotateX: 0, z: 0, scale: 1,
duration: 0.6, stagger: 0.03, ease: 'back.out(1.5)',
onComplete: function() { titleEl.classList.add('animated'); }
});
}
var navEl = document.getElementById('nav');
if (navEl) {
gsap.fromTo(navEl, { y: -80, opacity: 0 }, { y: 0, opacity: 1, duration: 0.8, delay: 0.5, ease: 'power3.out' });
}
if (!isMobile) {
var heroGlow1 = document.querySelector('.hero-glow-1');
var heroGlow2 = document.querySelector('.hero-glow-2');
var heroGrid = document.querySelector('.hero-grid');
if (heroGlow1) gsap.to(heroGlow1, { scrollTrigger: { trigger: '.hero', start: 'top top', end: 'bottom top', scrub: 1.5 }, y: 150, scale: 1.4, opacity: 0, ease: 'none' });
if (heroGlow2) gsap.to(heroGlow2, { scrollTrigger: { trigger: '.hero', start: 'top top', end: 'bottom top', scrub: 1.5 }, y: 100, scale: 0.6, opacity: 0, ease: 'none' });
if (heroGrid) gsap.to(heroGrid, { scrollTrigger: { trigger: '.hero', start: 'top top', end: 'bottom top', scrub: 1 }, y: 80, opacity: 0, ease: 'none' });
}
var heroStats = document.querySelectorAll('.hero-stat');
if (heroStats.length) {
gsap.fromTo(heroStats, { opacity: 0, y: 30, scale: 0.8 }, { opacity: 1, y: 0, scale: 1, duration: 0.6, stagger: 0.15, delay: 1.3, ease: 'back.out(1.7)' });
}
var about = document.querySelector('.about-intro');
if (about) {
gsap.fromTo(about.querySelector('.sec-label'), { opacity: 0, x: isMobile ? -30 : -80, rotateY: isMobile ? 0 : 15 }, { scrollTrigger: { trigger: about, start: 'top 85%', once: true }, opacity: 1, x: 0, rotateY: 0, duration: 0.8, ease: 'power3.out' });
apply3dChars(about.querySelector('.sec-title'), about);
var aboutPs = about.querySelectorAll('.about-inner p');
gsap.fromTo(aboutPs, { opacity: 0, y: isMobile ? 30 : 60, rotateX: isMobile ? 0 : 8 }, { scrollTrigger: { trigger: about, start: 'top 80%', once: true }, opacity: 1, y: 0, rotateX: 0, duration: 0.8, stagger: 0.2, ease: 'power2.out' });
}
var refsSection = document.querySelector('.refs');
if (refsSection) {
gsap.fromTo(refsSection.querySelector('.sec-label'), { opacity: 0, x: -80, rotateY: 15 }, { scrollTrigger: { trigger: refsSection, start: 'top 85%', once: true }, opacity: 1, x: 0, rotateY: 0, duration: 0.8, ease: 'power3.out' });
apply3dChars(refsSection.querySelector('.sec-title'), refsSection);
var refsDesc = refsSection.querySelector('.refs-header-desc');
if (refsDesc) gsap.fromTo(refsDesc, { opacity: 0, y: 30, x: 40 }, { scrollTrigger: { trigger: refsSection, start: 'top 85%', once: true }, opacity: 1, y: 0, x: 0, duration: 0.8, delay: 0.3, ease: 'power2.out' });
var refsCarousel = refsSection.querySelector('.refs-carousel');
if (refsCarousel) gsap.fromTo(refsCarousel, { opacity: 0, y: 60 }, { scrollTrigger: { trigger: refsCarousel, start: 'top 92%', once: true }, opacity: 1, y: 0, duration: 1, ease: 'power3.out' });
}
var propGrid = document.querySelector('.prop-grid');
var propSection = document.querySelector('.properties');
if (propSection) {
gsap.fromTo(propSection.querySelectorAll('.sec-label, .sec-sub'), { opacity: 0, y: 40, clipPath: 'inset(100% 0 0 0)' }, { scrollTrigger: { trigger: propSection, start: 'top 85%', once: true }, opacity: 1, y: 0, clipPath: 'inset(0% 0 0 0)', duration: 0.8, stagger: 0.1, ease: 'power3.out' });
apply3dChars(propSection.querySelector('.sec-title'), propSection);
var filterBtns = propSection.querySelectorAll('.prop-filters button');
if (filterBtns.length) gsap.fromTo(filterBtns, { opacity: 0, scale: 0.7, y: 20 }, { scrollTrigger: { trigger: propSection, start: 'top 85%', once: true }, opacity: 1, scale: 1, y: 0, duration: 0.5, stagger: 0.08, delay: 0.3, ease: 'back.out(2)' });
}
if (propGrid) {
var propCards = propGrid.querySelectorAll('.prop-card');
propCards.forEach(function(card, i){
if (isMobile) {
gsap.fromTo(card, { opacity: 0, y: 40, scale: 0.95 }, { scrollTrigger: { trigger: card, start: 'top 92%', once: true }, opacity: 1, y: 0, scale: 1, duration: 0.6, ease: 'power2.out' });
} else {
gsap.fromTo(card, { opacity: 0, y: 120, rotateX: 20, scale: 0.85, transformPerspective: 1200 }, { scrollTrigger: { trigger: propGrid, start: 'top 85%', once: true }, opacity: 1, y: 0, rotateX: 0, scale: 1, duration: 1, delay: i * 0.15, ease: 'power3.out' });
}
});
}
var srvGrid = document.querySelector('.srv-grid');
var srvSection = document.querySelector('.services-row');
if (srvSection) {
gsap.fromTo(srvSection.querySelector('.sec-label'), { opacity: 0, y: 40, clipPath: 'inset(100% 0 0 0)' }, { scrollTrigger: { trigger: srvSection, start: 'top 85%', once: true }, opacity: 1, y: 0, clipPath: 'inset(0% 0 0 0)', duration: 0.8, ease: 'power3.out' });
apply3dChars(srvSection.querySelector('.sec-title'), srvSection);
}
if (srvGrid) {
var srvItems = srvGrid.querySelectorAll('.srv-item');
if (isMobile) {
srvItems.forEach(function(item){
gsap.fromTo(item, { opacity: 0, y: 40 }, { scrollTrigger: { trigger: item, start: 'top 92%', once: true }, opacity: 1, y: 0, duration: 0.6, ease: 'power2.out' });
});
} else {
srvItems.forEach(function(item, i){
gsap.fromTo(item, { opacity: 0, y: 80, rotateY: i % 2 === 0 ? -15 : 15, scale: 0.85, transformPerspective: 800 }, {
scrollTrigger: { trigger: srvGrid, start: 'top 78%', once: true },
opacity: 1, y: 0, rotateY: 0, scale: 1, duration: 0.9, delay: i * 0.18, ease: 'power3.out'
});
});
}
}
var srvDetail = document.querySelector('.srv-detail-grid');
if (srvDetail) {
var srvBlocks = srvDetail.querySelectorAll('.srv-block');
srvBlocks.forEach(function(block, i){
if (isMobile) {
gsap.fromTo(block, { opacity: 0, y: 30 }, { scrollTrigger: { trigger: block, start: 'top 92%', once: true }, opacity: 1, y: 0, duration: 0.6, ease: 'power2.out' });
} else {
gsap.fromTo(block, { opacity: 0, rotateX: -25, y: 60, transformOrigin: 'top center', transformPerspective: 1000 }, { scrollTrigger: { trigger: block, start: 'top 88%', once: true }, opacity: 1, rotateX: 0, y: 0, duration: 1, ease: 'power3.out' });
}
var listItems = block.querySelectorAll('.srv-list li');
if (listItems.length) {
gsap.fromTo(listItems, { opacity: 0, x: -25 }, {
scrollTrigger: { trigger: block, start: 'top 75%', once: true },
opacity: 1, x: 0, duration: 0.4, stagger: 0.08, delay: isMobile ? 0.2 : 0.4, ease: 'power2.out'
});
}
});
}
var engageGrid = document.querySelector('.engage-grid');
var engageSection = document.querySelector('.why');
if (engageSection) {
gsap.fromTo(engageSection.querySelector('.sec-label'), { opacity: 0, y: 40, clipPath: 'inset(100% 0 0 0)' }, { scrollTrigger: { trigger: engageSection, start: 'top 85%', once: true }, opacity: 1, y: 0, clipPath: 'inset(0% 0 0 0)', duration: 0.9, ease: 'power3.out' });
apply3dChars(engageSection.querySelector('.sec-title'), engageSection);
}
if (engageGrid) {
var engCards = engageGrid.querySelectorAll('.engage-card');
if (isMobile) {
engCards.forEach(function(card){
gsap.fromTo(card, { opacity: 0, y: 30, scale: 0.95 }, { scrollTrigger: { trigger: card, start: 'top 92%', once: true }, opacity: 1, y: 0, scale: 1, duration: 0.6, ease: 'power2.out' });
});
} else {
engCards.forEach(function(card, i){
gsap.fromTo(card, { opacity: 0, y: 80, scale: 0.7, rotateX: 20, transformPerspective: 1000 }, {
scrollTrigger: { trigger: engageGrid, start: 'top 82%', once: true },
opacity: 1, y: 0, scale: 1, rotateX: 0, duration: 0.9, delay: i * 0.12, ease: 'elastic.out(1, 0.75)'
});
});
}
}
var ambitionBox = document.querySelector('.ambition-box');
if (ambitionBox) {
gsap.fromTo(ambitionBox, { opacity: 0, scaleX: isMobile ? 1 : 0.7, y: isMobile ? 30 : 0, rotateY: isMobile ? 0 : -12, transformPerspective: 1000 }, { scrollTrigger: { trigger: ambitionBox, start: 'top 88%', once: true }, opacity: 1, scaleX: 1, y: 0, rotateY: 0, duration: 1.1, ease: 'power3.out' });
}
var statsGrid = document.querySelector('.stats-grid');
if (statsGrid) {
var statItems = statsGrid.querySelectorAll('.st');
statItems.forEach(function(st, i){
gsap.fromTo(st, { opacity: 0, scale: isMobile ? 0.8 : 0.3, y: 40, rotateZ: isMobile ? 0 : (i % 2 === 0 ? -5 : 5) }, {
scrollTrigger: { trigger: isMobile ? st : statsGrid, start: 'top 88%', once: true },
opacity: 1, scale: 1, y: 0, rotateZ: 0, duration: 0.8, delay: isMobile ? 0 : i * 0.12, ease: 'back.out(1.7)'
});
});
}
var ctaSection = document.querySelector('.cta');
if (ctaSection) {
if (isMobile) {
gsap.fromTo(ctaSection.children, { opacity: 0, y: 30 }, { scrollTrigger: { trigger: ctaSection, start: 'top 88%', once: true }, opacity: 1, y: 0, duration: 0.8, stagger: 0.15, ease: 'power3.out' });
} else {
gsap.fromTo(ctaSection.children, { opacity: 0, y: 60, scale: 0.9, rotateX: 8, transformPerspective: 800 }, {
scrollTrigger: { trigger: ctaSection, start: 'top 85%', once: true },
opacity: 1, y: 0, scale: 1, rotateX: 0, duration: 1, stagger: 0.15, ease: 'power3.out'
});
gsap.fromTo(ctaSection, { scale: 0.95 }, {
scrollTrigger: { trigger: ctaSection, start: 'top bottom', end: 'top 50%', scrub: 1 },
scale: 1, ease: 'none'
});
}
}
var contactSection = document.querySelector('.contact');
if (contactSection) {
gsap.fromTo(contactSection.querySelector('.sec-label'), { opacity: 0, y: 40, clipPath: 'inset(100% 0 0 0)' }, { scrollTrigger: { trigger: contactSection, start: 'top 85%', once: true }, opacity: 1, y: 0, clipPath: 'inset(0% 0 0 0)', duration: 0.8, ease: 'power3.out' });
apply3dChars(contactSection.querySelector('.sec-title'), contactSection);
var ctLocCard = contactSection.querySelector('.ct-location-card');
var ctFormCard = contactSection.querySelector('.ct-form-card');
if (isMobile) {
if (ctLocCard) gsap.fromTo(ctLocCard, { opacity: 0, y: 40 }, { scrollTrigger: { trigger: ctLocCard, start: 'top 92%', once: true }, opacity: 1, y: 0, duration: 0.7, ease: 'power2.out' });
if (ctFormCard) gsap.fromTo(ctFormCard, { opacity: 0, y: 40 }, { scrollTrigger: { trigger: ctFormCard, start: 'top 92%', once: true }, opacity: 1, y: 0, duration: 0.7, ease: 'power2.out' });
} else {
if (ctLocCard) gsap.fromTo(ctLocCard, { opacity: 0, x: -80, rotateY: 12, scale: 0.9, transformPerspective: 1200 }, { scrollTrigger: { trigger: ctLocCard, start: 'top 85%', once: true }, opacity: 1, x: 0, rotateY: 0, scale: 1, duration: 1, ease: 'power3.out' });
if (ctFormCard) gsap.fromTo(ctFormCard, { opacity: 0, x: 80, rotateY: -12, scale: 0.9, transformPerspective: 1200 }, { scrollTrigger: { trigger: ctFormCard, start: 'top 85%', once: true }, opacity: 1, x: 0, rotateY: 0, scale: 1, duration: 1, delay: 0.15, ease: 'power3.out' });
}
var formGroups = contactSection.querySelectorAll('.c-group');
if (formGroups.length) {
gsap.fromTo(formGroups, { opacity: 0, y: 25, scale: 0.95 }, {
scrollTrigger: { trigger: ctFormCard || contactSection, start: 'top 80%', once: true },
opacity: 1, y: 0, scale: 1, duration: 0.5, stagger: 0.08, delay: 0.5, ease: 'power2.out'
});
}
var ctLocItems = contactSection.querySelectorAll('.ct-loc-item');
if (ctLocItems.length) {
gsap.fromTo(ctLocItems, { opacity: 0, x: -40 }, {
scrollTrigger: { trigger: ctLocCard || contactSection, start: 'top 80%', once: true },
opacity: 1, x: 0, duration: 0.5, stagger: 0.12, delay: 0.5, ease: 'power2.out'
});
}
}
var footer = document.querySelector('footer');
if (footer) {
var footEls = footer.querySelectorAll('.foot-brand, .foot-col, .foot-bottom');
gsap.fromTo(footEls, { opacity: 0, y: 40 }, {
scrollTrigger: { trigger: footer, start: 'top 92%', once: true },
opacity: 1, y: 0, duration: 0.7, stagger: 0.12, ease: 'power2.out'
});
}
function animateCounter(el, target, suffix) {
var obj = { val: 0 };
gsap.to(obj, {
val: target, duration: 2, ease: 'power1.out',
scrollTrigger: { trigger: el, start: 'top 90%', once: true },
onUpdate: function(){ el.textContent = Math.round(obj.val) + suffix; }
});
}
document.querySelectorAll('.hero-stat-num').forEach(function(el){
var text = el.textContent.trim();
var num = parseInt(text);
if (isNaN(num)) return;
var suffix = text.replace(/[0-9]/g, '');
el.textContent = '0' + suffix;
var obj = { val: 0 };
gsap.to(obj, {
val: num, duration: 2, delay: 0.5, ease: 'power1.out',
onUpdate: function(){ el.textContent = Math.round(obj.val) + suffix; }
});
});
document.querySelectorAll('.st-num').forEach(function(el){
var text = el.textContent.trim();
var num = parseInt(text);
if (isNaN(num)) return;
var suffix = text.replace(/[0-9]/g, '');
el.textContent = '0' + suffix;
animateCounter(el, num, suffix);
});
} else {
document.body.classList.add('no-gsap');
var obs = new IntersectionObserver(function(entries){
entries.forEach(function(e){
if (e.isIntersecting) e.target.classList.add('show');
});
}, { threshold: 0.08, rootMargin: '0px 0px -40px 0px' });
document.querySelectorAll('.rv').forEach(function(el){ obs.observe(el); });
}
document.querySelectorAll('.tilt').forEach(function(card){
card.addEventListener('mousemove', function(e){
var r = card.getBoundingClientRect();
var x = (e.clientX - r.left) / r.width - 0.5;
var y = (e.clientY - r.top) / r.height - 0.5;
card.style.transform = 'perspective(800px) rotateX(' + (y * -10) + 'deg) rotateY(' + (x * 10) + 'deg) scale(1.02)';
});
card.addEventListener('mouseleave', function(){
card.style.transform = 'perspective(800px) rotateX(0) rotateY(0) scale(1)';
});
});
var floatBaseTransforms = [
'rotateY(-8deg) rotateX(3deg)',
'rotateY(-5deg) rotateX(-2deg)',
'rotateY(-10deg) rotateX(5deg)'
];
document.addEventListener('mousemove', function(e){
var x = (e.clientX / innerWidth - 0.5) * 2;
var y = (e.clientY / innerHeight - 0.5) * 2;
document.querySelectorAll('.float-card').forEach(function(c, i){
var sp = (i + 1) * 12;
c.style.transform = (floatBaseTransforms[i] || '') + ' translate(' + (x * sp) + 'px,' + (y * sp) + 'px)';
});
});
var marqueeSection = document.getElementById('marqueeSection');
if (marqueeSection && typeof gsap !== 'undefined' && typeof ScrollTrigger !== 'undefined') {
gsap.from(marqueeSection, {
scrollTrigger: { trigger: marqueeSection, start: 'top 88%', once: true },
opacity: 0, y: 40, scaleX: 0.9, duration: 0.9, ease: 'power2.out'
});
var bgText = marqueeSection.querySelector('.marquee-bg-text');
if (bgText) {
gsap.to(bgText, {
scrollTrigger: { trigger: marqueeSection, start: 'top bottom', end: 'bottom top', scrub: 1.5 },
x: 120, scale: 1.1, ease: 'none'
});
}
}
document.querySelectorAll('.prop-filters button').forEach(function(btn){
btn.addEventListener('click', function(){
document.querySelectorAll('.prop-filters button').forEach(function(b){ b.classList.remove('active'); });
btn.classList.add('active');
var filter = btn.getAttribute('data-filter');
document.querySelectorAll('.prop-card').forEach(function(card, i){
if (filter === 'all' || card.getAttribute('data-type') === filter) {
card.style.display = '';
if (typeof gsap !== 'undefined') {
gsap.fromTo(card, { opacity: 0, y: 30, scale: 0.95 }, { opacity: 1, y: 0, scale: 1, duration: 0.5, delay: i * 0.08, ease: 'power2.out' });
}
} else {
if (typeof gsap !== 'undefined') {
gsap.to(card, { opacity: 0, scale: 0.9, duration: 0.3, ease: 'power2.in', onComplete: function(){ card.style.display = 'none'; } });
} else {
card.style.display = 'none';
}
}
});
});
});
document.querySelectorAll('a[href^="#"]').forEach(function(a){
a.addEventListener('click', function(e){
e.preventDefault();
var t = document.querySelector(a.getAttribute('href'));
if (t) t.scrollIntoView({ behavior: 'smooth' });
});
});
var overlay = document.getElementById('modalOverlay');
var propData = (typeof lcProperties !== 'undefined') ? lcProperties : [];
var waNum = (typeof lcData !== 'undefined') ? lcData.whatsapp : '212700727165';
var emailAddr = (typeof lcData !== 'undefined') ? lcData.email : 'ezine.surgar@gmail.com';
document.querySelectorAll('.prop-card').forEach(function(card, i){
card.addEventListener('click', function(){
var d = propData[i];
if (!d) return;
document.getElementById('modalIcon').textContent = d.icon;
var badge = document.getElementById('modalBadge');
badge.textContent = d.badge;
badge.className = 'm-badge ' + d.badgeClass;
document.getElementById('modalPrice').textContent = d.price;
document.getElementById('modalTitle').textContent = d.title;
document.getElementById('modalLoc').textContent = d.loc;
document.getElementById('modalRef').textContent = d.ref;
var specs = document.getElementById('modalSpecs');
specs.textContent = '';
for (var j = 0; j < d.specs.length; j += 2) {
var ms = document.createElement('div'); ms.className = 'ms';
var msVal = document.createElement('div'); msVal.className = 'ms-val'; msVal.textContent = d.specs[j];
var msLbl = document.createElement('div'); msLbl.className = 'ms-lbl'; msLbl.textContent = d.specs[j+1];
ms.appendChild(msVal); ms.appendChild(msLbl); specs.appendChild(ms);
}
document.getElementById('modalDesc').textContent = d.desc;
var feats = document.getElementById('modalFeatures');
feats.textContent = '';
d.features.forEach(function(f){ var mf = document.createElement('div'); mf.className = 'mf'; mf.textContent = f; feats.appendChild(mf); });
var waBtn = document.getElementById('modalWaBtn');
if (waBtn) waBtn.href = 'https://wa.me/' + waNum + '?text=' + encodeURIComponent('Bonjour, je suis intéressé par le bien ' + d.ref + ' — ' + d.title);
overlay.classList.add('open');
document.body.style.overflow = 'hidden';
});
});
if (overlay) {
var modalClose = document.getElementById('modalClose');
if (modalClose) {
modalClose.addEventListener('click', function(){
overlay.classList.remove('open');
document.body.style.overflow = '';
});
}
overlay.addEventListener('click', function(e){
if (e.target === overlay) {
overlay.classList.remove('open');
document.body.style.overflow = '';
}
});
document.addEventListener('keydown', function(e){
if (e.key === 'Escape' && overlay.classList.contains('open')) {
overlay.classList.remove('open');
document.body.style.overflow = '';
}
});
}
var backToTop = document.getElementById('backToTop');
if (backToTop) {
window.addEventListener('scroll', function(){
backToTop.classList.toggle('visible', window.scrollY > 500);
});
backToTop.addEventListener('click', function(){
window.scrollTo({ top: 0, behavior: 'smooth' });
});
}
var mapIframe = document.querySelector('.ct-loc-map iframe[data-src]');
if (mapIframe) {
if ('IntersectionObserver' in window) {
var mapObs = new IntersectionObserver(function(entries){
entries.forEach(function(e){
if (e.isIntersecting) {
e.target.src = e.target.dataset.src;
mapObs.unobserve(e.target);
}
});
}, { rootMargin: '300px' });
mapObs.observe(mapIframe);
} else {
mapIframe.src = mapIframe.dataset.src;
}
}
function showToast(message, type) {
var existing = document.querySelector('.toast');
if (existing) existing.remove();
var toast = document.createElement('div');
toast.className = 'toast' + (type === 'email' ? ' toast-email' : '');
toast.innerHTML = '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>' + message;
document.body.appendChild(toast);
requestAnimationFrame(function(){
requestAnimationFrame(function(){ toast.classList.add('show'); });
});
setTimeout(function(){ toast.classList.remove('show'); setTimeout(function(){ toast.remove(); }, 400); }, 3500);
}
function validateField(field) {
if (!field) return true;
var valid = true;
if ((field.hasAttribute('required') || field.getAttribute('aria-required') === 'true') && !field.value.trim()) valid = false;
if (field.type === 'tel' && field.value.trim() && !/^[\d\s\+\-\.()]{6,}$/.test(field.value.trim())) valid = false;
field.classList.toggle('invalid', !valid);
return valid;
}
var cfName = document.getElementById('cfName');
var cfPhone = document.getElementById('cfPhone');
if (cfName) {
cfName.addEventListener('blur', function(){ validateField(cfName); });
cfName.addEventListener('input', function(){ if (cfName.value.trim()) cfName.classList.remove('invalid'); });
}
if (cfPhone) {
cfPhone.addEventListener('blur', function(){ validateField(cfPhone); });
cfPhone.addEventListener('input', function(){ if (cfPhone.value.trim()) cfPhone.classList.remove('invalid'); });
}
function getFieldValue(id){
var field = document.getElementById(id);
return field ? field.value.trim() : '';
}
function getFormData(){
return {
name: getFieldValue('cfName'),
phone: getFieldValue('cfPhone'),
type: getFieldValue('cfType'),
budget: getFieldValue('cfBudget'),
msg: getFieldValue('cfMsg')
};
}
function formatMsg(d){
return 'Bonjour Luxury Copro,\n\nNom: ' + d.name + '\nTéléphone: ' + d.phone + '\nProjet: ' + d.type + '\nBudget: ' + d.budget + (d.msg ? '\nMessage: ' + d.msg : '');
}
var contactForm = document.getElementById('contactForm');
if (contactForm) {
contactForm.addEventListener('submit', function(e){
e.preventDefault();
var nameOk = validateField(document.getElementById('cfName'));
var phoneOk = validateField(document.getElementById('cfPhone'));
if (!nameOk || !phoneOk) {
contactForm.classList.add('shake');
setTimeout(function(){ contactForm.classList.remove('shake'); }, 500);
return;
}
var d = getFormData();
var text = encodeURIComponent(formatMsg(d));
window.open('https://wa.me/' + waNum + '?text=' + text, '_blank');
showToast('Message envoyé via WhatsApp !');
e.target.reset();
});
}
var emailBtn = document.getElementById('cfEmailBtn');
if (emailBtn) {
emailBtn.addEventListener('click', function(){
var nameOk = validateField(document.getElementById('cfName'));
var phoneOk = validateField(document.getElementById('cfPhone'));
if (!nameOk || !phoneOk) {
contactForm.classList.add('shake');
setTimeout(function(){ contactForm.classList.remove('shake'); }, 500);
return;
}
var d = getFormData();
var subject = encodeURIComponent('Nouveau contact — ' + d.type + ' — ' + d.name);
var body = encodeURIComponent(formatMsg(d));
window.location.href = 'mailto:' + emailAddr + '?subject=' + subject + '&body=' + body;
showToast('Message préparé par e-mail !', 'email');
contactForm.reset();
});
}
var cf7WhatsAppBtn = document.getElementById('cf7WhatsAppBtn');
var cf7ContactForm = document.querySelector('.lc-cf7-form');
if (cf7WhatsAppBtn && cf7ContactForm) {
cf7WhatsAppBtn.addEventListener('click', function(){
var nameOk = validateField(document.getElementById('cfName'));
var phoneOk = validateField(document.getElementById('cfPhone'));
if (!nameOk || !phoneOk) {
cf7ContactForm.classList.add('shake');
setTimeout(function(){ cf7ContactForm.classList.remove('shake'); }, 500);
return;
}
var d = getFormData();
window.open('https://wa.me/' + waNum + '?text=' + encodeURIComponent(formatMsg(d)), '_blank');
showToast('Message envoyé via WhatsApp !');
});
}
document.addEventListener('wpcf7mailsent', function(e){
if (e.target && e.target.classList && e.target.classList.contains('lc-cf7-form')) {
showToast('Message envoyé par e-mail !', 'email');
}
});
var waWidget = document.getElementById('waWidget');
var waFab = document.getElementById('waFab');
if (waFab && waWidget) {
waFab.addEventListener('click', function(){ waWidget.classList.toggle('open'); });
var waSend = document.getElementById('waSend');
var waInput = document.getElementById('waInput');
if (waSend && waInput) {
waSend.addEventListener('click', function(){
var msg = waInput.value.trim();
if (!msg) return;
window.open('https://wa.me/' + waNum + '?text=' + encodeURIComponent(msg), '_blank');
waInput.value = '';
waWidget.classList.remove('open');
});
waInput.addEventListener('keydown', function(e){
if (e.key === 'Enter') waSend.click();
});
}
}
(function(){
var sig = document.getElementById('footSignature');
var link = document.getElementById('footSigLink');
var canvas = document.getElementById('footSigParticles');
if (!sig || !link) return;
var shimmer = document.createElement('span');
shimmer.className = 'foot-sig-shimmer';
shimmer.setAttribute('aria-hidden', 'true');
link.appendChild(shimmer);
if (typeof gsap !== 'undefined' && typeof ScrollTrigger !== 'undefined' && !prefersReducedMotion) {
var sigLine = sig.querySelector('.foot-sig-line');
var sigSub = sig.querySelector('.foot-sig-sub');
gsap.fromTo(sigLine, { scaleX: 0 }, { scrollTrigger: { trigger: sig, start: 'top 95%', once: true }, scaleX: 1, duration: .8, ease: 'power3.out' });
gsap.fromTo(link, { opacity: 0, y: 20, filter: 'blur(8px)' }, { scrollTrigger: { trigger: sig, start: 'top 95%', once: true }, opacity: 1, y: 0, filter: 'blur(0px)', duration: .9, delay: .3, ease: 'power3.out' });
gsap.fromTo(sigSub, { opacity: 0, y: 10 }, { scrollTrigger: { trigger: sig, start: 'top 95%', once: true }, opacity: .5, y: 0, duration: .7, delay: .6, ease: 'power2.out' });
}
if (!canvas || prefersReducedMotion) return;
var ctx = canvas.getContext('2d');
var particles = [];
var hoverCount = 0;
var burstActive = false;
function resizeCanvas() {
var rect = sig.getBoundingClientRect();
canvas.width = rect.width;
canvas.height = rect.height;
}
resizeCanvas();
window.addEventListener('resize', resizeCanvas);
function spawnBurst(x, y) {
for (var i = 0; i < 24; i++) {
var angle = (Math.PI * 2 / 24) * i + (Math.random() - .5) * .5;
var speed = 1.2 + Math.random() * 2.5;
particles.push({
x: x, y: y,
vx: Math.cos(angle) * speed,
vy: Math.sin(angle) * speed,
life: 1,
decay: .012 + Math.random() * .012,
size: 1.5 + Math.random() * 2,
hue: 200 + Math.random() * 30
});
}
burstActive = true;
animateParticles();
}
function animateParticles() {
if (!burstActive) return;
ctx.clearRect(0, 0, canvas.width, canvas.height);
var alive = false;
for (var i = particles.length - 1; i >= 0; i--) {
var p = particles[i];
p.x += p.vx;
p.y += p.vy;
p.vy += .02;
p.vx *= .99;
p.life -= p.decay;
if (p.life <= 0) { particles.splice(i, 1); continue; }
alive = true;
ctx.save();
ctx.globalAlpha = p.life * .7;
ctx.fillStyle = 'hsla(' + p.hue + ',60%,65%,' + p.life + ')';
ctx.shadowColor = 'hsla(' + p.hue + ',70%,60%,.5)';
ctx.shadowBlur = 6;
ctx.beginPath();
ctx.arc(p.x, p.y, p.size * p.life, 0, Math.PI * 2);
ctx.fill();
ctx.restore();
}
if (alive) {
requestAnimationFrame(animateParticles);
} else {
burstActive = false;
ctx.clearRect(0, 0, canvas.width, canvas.height);
}
}
link.addEventListener('mouseenter', function() {
hoverCount++;
if (hoverCount % 5 === 0) {
resizeCanvas();
var rect = link.getBoundingClientRect();
var sigRect = sig.getBoundingClientRect();
var cx = rect.left - sigRect.left + rect.width / 2;
var cy = rect.top - sigRect.top + rect.height / 2;
spawnBurst(cx, cy);
}
});
})();
if ('IntersectionObserver' in window) {
var lazyBgs = document.querySelectorAll('.lazy-bg');
var bgObs = new IntersectionObserver(function(entries){
entries.forEach(function(e){
if (e.isIntersecting) {
e.target.style.backgroundImage = e.target.dataset.bg;
bgObs.unobserve(e.target);
}
});
}, { rootMargin: '200px' });
lazyBgs.forEach(function(el){ bgObs.observe(el); });
}
})();
