# Match these to your host user from `id -u` and `id -g`.
# The defaults work for most Linux development machines.
WORDPRESS_UID=1000
WORDPRESS_GID=1000

# Set to true to load unminified theme assets while developing.
SCRIPT_DEBUG=false
